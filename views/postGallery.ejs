<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register page</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="overflow-x-hidden">
  <div class="w-screen min-h-screen h-max text-white py-2 bg-zinc-900 flex flex-col overflow-x-hidden justify-center items-center">
    <nav class="w-screen min-h-[5vh] flex justify-between items-center px-10 mb-5">
      <a  class="bg-blue-500 px-5 py-2 text-xl rounded-md" href="/">Profile</a>
      <a  class="bg-red-500 px-5 py-2 text-xl rounded-md" href="/logout">Logout</a>
    </nav>
    <!-- users posts and edit and like button -->
    <div
      class="post-container w-screen min-h-screen h-max place-items-center justify-center grid sm:gap-2 gap-7 grid xl:grid-cols-4 lg:grid-cols-3 sm:grid-cols-2 grid-cols-1  p-6 rounded-lg">
      <%if(!posts.length>0){%>
        <h1 class="text-zinc-500">There is no post yet</h1>
        <%}else{%>
          <%posts.forEach((post)=>{%>
            <div
             class="w-[90%] h-[300px] bg-zinc-800  p-5 rounded-xl shadow-lg shadow-slate-800">
              <h3 class="p-2 text-left text-zinc-500 mb-2 text-sm tracking-wide ">@<%=post.user.username%></h3>
              <h1 class="p-2 text-center text-zinc-200 mb-2 text-xl tracking-tight capitalize"><%=post.title%></h1>
              <p class="p-2 w-[100%] h-[75px] inline-block text-center text-zinc-400 mb-2 text-md tracking-tighter overflow-hidden"><%=post.content%></p>
              <small><%= post.Likes.length%> likes</small>
                <a href="/all/like/<%= post._id %>" class="text-white bg-blue-500 px-4 py-2 rounded-xl">
                    <%=post.Likes.indexOf(user._id)==-1?"Like":"Unlike" %>
                </a>
            </div>
          <%})%>
        <%} %>
    </div>
  </div>
</body>

</html>